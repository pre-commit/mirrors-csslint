-   id: csslint
    name: csslint
    entry: csslint
    language: node
    files: \.css$
    args: []

    # Use additional_dependencies to install the actual node package
    additional_dependencies: ['csslint@0.10.0']
    minimum_pre_commit_version: 0.7.0
